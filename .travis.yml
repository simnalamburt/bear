language: rust
os:
  - linux
  - osx
notifications:
  irc:
    nick: bear-rs
    channels:
      - irc.uriirc.org:16667#hyeon
    template:
      - "#%{build_number} %{result}, %{build_url}"
    skip_join: true
